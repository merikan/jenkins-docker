pipeline {
  agent {
    docker {
      image 'eclipse-temurin:17-jdk-centos7'
    }
  }
  stages {
    stage('no agent') {
      steps {
        sh(script: 'java -version', returnStdout: true)
      }
    }
  }
}
