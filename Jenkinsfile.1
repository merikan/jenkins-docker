pipeline {
  agent {
    docker {
      image 'openjdk:17'
      //image 'eclipse-temurin:17-jdk'
    }
  }
  stages {
    stage('no agent') {
      steps {
        sh(script: 'java -version', returnStdout: true)
      }
    }
  }
}
